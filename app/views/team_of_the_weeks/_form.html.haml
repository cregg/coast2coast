= form_for @team_of_the_week do |f|
  - if @team_of_the_week.errors.any?
    #error_explanation
      %h2= "#{pluralize(@team_of_the_week.errors.count, "error")} prohibited this team_of_the_week from being saved:"
      %ul
        - @team_of_the_week.errors.full_messages.each do |msg|
          %li= msg

  .actions
    = f.submit 'Save'
